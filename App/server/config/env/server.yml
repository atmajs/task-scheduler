
env:
	server:


		routes:
			server : '/server/{0}.js' 
			server_lib : '/server/lib/{0}.js' 
			server_compo : '/server/compo/{0}/{1}.js'

			Project: '../{0}/{0}.js'
		scripts:
			
			Project:
				# alias is taken from filename for `include.use(ALIAS)`
				# so it same as, e.g 'Utils::Utils'
				
				- 'Utils'
				- 'Model'
				- 'Queue'
				- 'Worker'
			
